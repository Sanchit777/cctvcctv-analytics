{
    "installCommand": "pip install -r reqs.txt --target . && find . -name 'tests' -type d -exec rm -rf {} + && rm -rf $(pip cache dir)",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/main.py"
        }
    ]
}