{
    "installCommand": "mkdir -p packages && pip install -r reqs.txt --target packages && find packages -name 'tests' -type d -exec rm -rf {} + && find packages -name 'docs' -type d -exec rm -rf {} + && find packages -name '__pycache__' -type d -exec rm -rf {} + && rm -rf $(pip cache dir)",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/main.py"
        }
    ]
}