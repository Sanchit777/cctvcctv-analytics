{
    "installCommand": "ls -la && if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; else pip install -r requirements.txt; fi && find . -name 'tests' -type d -exec rm -rf {} + && find . -name '__pycache__' -type d -exec rm -rf {} +",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/main.py"
        }
    ]
}