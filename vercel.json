{
    "installCommand": "ls -R && pip install -r backend/reqs.txt || pip install -r reqs.txt && find . -name 'tests' -type d -exec rm -rf {} + && rm -rf $(pip cache dir)",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/main.py"
        }
    ]
}