{
    "installCommand": "pip install -r requirements.txt || pip install -r backend/requirements.txt && find . -name 'tests' -type d -exec rm -rf {} + && rm -rf $(pip cache dir)",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/main.py"
        }
    ]
}